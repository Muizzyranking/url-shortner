# AI Usage Log

| Tool used | What you asked / generated | What you changed / decided yourself |
|---|---|---|
| Claude | Scaffold the full URL shortener from a written spec: FastAPI + Postgres + Redis, required endpoints (`POST/GET /api/links`, `GET /:slug` redirect, `DELETE /api/links/:slug`), Dockerfile on port 8080, README, input validation returning 400 not 500. | Specified the stack decisions myself up front: sync `DATABASE_URL` needing to be rewritten for `asyncpg`, Redis already provisioned for caching/rate limiting, custom in-house rate limiter (fastlimit) to be wired in later — told Claude not to build it. Confirmed the async-over-sync architecture call Claude proposed (sync psycopg2 + sync routes would block the event loop) before letting it scaffold. |
| Claude | Asked it to design the `DATABASE_URL` → `asyncpg` rewrite logic, including handling Heroku's legacy `postgres://` scheme and Neon's `sslmode=require`/`channel_binding` query params that `asyncpg` doesn't understand. | Accepted the rewrite logic as generated. Kept the `ssl_required` computed flag approach (deriving `connect_args={"ssl": "require"}` for the engine) rather than leaving `sslmode` in the URL, since Claude explained asyncpg would reject it outright. |
| Claude | Asked for business logic to live in a `services/` layer with thin routers, background-task-based click logging (FastAPI `BackgroundTasks`, not Celery). | Set this architectural constraint myself (routers stay lean, services do the heavy lifting) before generation started. Accepted the resulting split (`link_service.py`, `click_service.py`) as-is. |
| Claude | Asked for UX-level rework of the UI: replace a raw "expires in seconds" number input with friendlier presets (Never/1h/1d/1w/30d/Custom), add client-side validation with inline field errors, map server-side field errors onto the actual inputs instead of dumping raw JSON in a banner, make the table responsive (collapse to cards on mobile), add a favicon. | Directed the specific UX complaints myself (the seconds field, the raw-JSON error banner, lack of responsiveness) rather than leaving it to Claude's judgment. |
| Claude | Asked to migrate the whole project from `pip`/`requirements.txt` to `uv` (`pyproject.toml` + committed `uv.lock`), update the Dockerfile and CI workflow accordingly, and add a persistent named volume for Postgres data in `docker-compose.yml` (previously missing). | Caught the missing Postgres volume — pointed out data wasn't surviving `docker compose down`/restart, which the original compose file didn't handle. Accepted Claude's `uv`-based Dockerfile and CI rewrite after confirming it verified the change by running `uv sync`, `uv run pytest`, and `uv run ruff check` locally rather than just asserting it would work. |
